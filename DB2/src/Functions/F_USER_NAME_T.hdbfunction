FUNCTION "F_USER_NAME_T"( )
       RETURNS TABLE (
    			 	"USERNAME" NVARCHAR(50)
       )
       LANGUAGE SQLSCRIPT 
       SQL SECURITY INVOKER AS 
BEGIN 
    RETURN SELECT "FIRSTNAME" || ' ' || "LASTNAME"	AS "USERNAME"
	FROM "USERS";
END;