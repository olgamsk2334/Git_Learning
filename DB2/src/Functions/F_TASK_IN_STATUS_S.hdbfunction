FUNCTION "F_TASK_IN_STATUS_S"( 
	IV_ID INTEGER 
)
       RETURNS EV_STATUS INTEGER
       LANGUAGE SQLSCRIPT 
       SQL SECURITY INVOKER AS 
BEGIN 

    SELECT "STATUS" INTO EV_STATUS
    FROM "TASKS"
    WHERE "ID" = :IV_ID;
    
END;