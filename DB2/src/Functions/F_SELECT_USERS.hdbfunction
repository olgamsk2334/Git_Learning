FUNCTION "F_SELECT_USERS"( )
       RETURNS "USERS" 
       LANGUAGE SQLSCRIPT 
       SQL SECURITY INVOKER AS 
BEGIN 
   RETURN SELECT DISTINCT "U".*
		  FROM "USERS" AS "U"
		  WHERE "ID" > 10
		  ORDER BY FIRSTNAME
		  LIMIT 15 OFFSET 5;
		  
END;