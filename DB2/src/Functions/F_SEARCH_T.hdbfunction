FUNCTION "F_SEARCH_T"( )
       RETURNS  "USERS"
       LANGUAGE SQLSCRIPT 
       SQL SECURITY INVOKER AS 
BEGIN 
   RETURN SELECT U.* 
			FROM "USERS" AS U
			WHERE CONTAINS ( "FIRSTNAME", '%arby' );
END;