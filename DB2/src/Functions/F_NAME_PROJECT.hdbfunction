FUNCTION "F_NAME_PROJECT"(
						IV_STATUS INTEGER 
						)
       RETURNS "PROJECTS"
       LANGUAGE SQLSCRIPT 
       SQL SECURITY INVOKER AS 
BEGIN 
	RETURN	 SELECT *
		 FROM "PROJECTS"
		 WHERE "STATUS" = :IV_STATUS;
END;